/**
 * Returning Values Created by Phirom Yim on 4/29/14.
 */

var total = calcArea(30, 20);//assign it to function call

function calcArea(w, h) {//w=30, h=20
    var area = w * h;//variable for are
    return area;//what we are returning, function spitting info out
    //returning in the argument
}
console.log(total);
